<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.AlumnoClient" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/black</item> <!-- Color oscuro principal -->
        <item name="colorSecondary">@color/gray</item> <!-- Color oscuro secundario -->
        <item name="colorButtonNormal">@color/dark_gray</item>
        <item name="colorControlNormal">@color/white</item> <!-- Iconos y controles en blanco -->
        <item name="colorControlHighlight">@color/light_gray</item>
        <item name="android:fontFamily">@font/ubuntu_regular</item>
    </style>

    <style name="Theme.AlumnoClient" parent="Base.Theme.AlumnoClient" />
</resources>
